{
  "name": "class-consensus",
  "version": "1.0.0",
  "description": "Class Consensus Smart Contract",
  "scripts": {
    "test": "npx hardhat test",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.ts"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0",
    "typescript": ">=4.5.0",
    "ts-node": ">=8.0.0",
    "@types/node": ">=18.0.0",
    "@types/mocha": ">=9.1.0",
    "@types/chai": ">=4.2.0",
    "ethers": "^6.4.0",
    "dotenv": "^16.4.0"
  }
}
